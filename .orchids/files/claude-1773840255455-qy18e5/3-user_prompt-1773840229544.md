[18/03, 6:52 pm] Pradyuman: Task: Implement a dynamic data population system for the "Graced Students History" sheet that adapts to any selected Grace Rule/Ordinance.

1. Rule-Agnostic Header:

The title of the sheet (e.g., "ORDINANCE O.5042-A") should dynamically update based on the specific Grace Rule currently being processed or filtered.

2. Dynamic Data Mapping:

Regardless of the rule selected, ensure the system accurately populates:

Before INT / Before EXT: Marks of the student before any grace was applied.

Grace Added (@) INT / EXT: The exact numeric distribution of grace marks. It must show the specific addition (e.g., INT: 1, EXT: 1) instead of default zeros.

Total Grace Added (@): The sum of all grace marks awarded to that student for that specific rule.

3. Intelligent Row Merging (Subject-Based):

If a student is eligible for grace in multiple subjects under the selected rule:

List all subjects in separate rows within the 'Subject' column.

Vertically merge the cells for 'Roll No', 'Student Name', 'Total Grace Added', 'Ordinance Type', and 'Final Result' to group them under one student profile.

This merging must adjust automatically based on whether the student has 1, 2, or more subjects flagged.

4. Data Integrity & Formatting:

No Structural Changes: Do not alter the column order, colors, font styles, or row heights.

Accuracy: The 'Final Result' must be recalculated based on the specific logic of the selected Ordinance to ensure it shows the correct status (PASS/ATKT).

Zero Suppression: If no grace was added to a specific component (like INT), show '0', but ensure the 'Total' column reflects the actual sum.
[18/03, 6:53 pm] Pradyuman: Task: Redesign the "Graced Students History" web page UI to improve data hierarchy and remove redundancy, matching the logic of the Excel export.

1. Data Grouping (Remove Duplicate Rows):

Single Entry per Student: If a student has received grace marks in multiple subjects, do not repeat the Roll No and Student Name in separate rows.

Row Merging/Grouping: Display the Roll No and Name once, and list all associated subjects and their marks in a grouped format within that entry.

2. Column Reorganization:

Remove "Original Marks" Column: This is no longer needed as a standalone column.

Add "Internal" & "External" Columns: Replace the broad 'Grace Added' section with two specific columns: Internal and External.

Display Format: Inside these new columns, use the [Before Marks]+@[Grace] format (e.g., 12+@2) so the breakdown is immediately visible on the UI.

3. Summary Columns:

Total Grace Marks: Add a column to show the total grace marks awarded across all subjects for that student.

Ordinance Type: Keep this column but ensure it is aligned with the grouped student data.

4. Visual Layout (UI/UX):

Subject List: When a student has multiple subjects, list them vertically within the "Subject" cell of the grouped row.

Clarity: Ensure that even with the +@ notation, the table remains readable and doesn't look cramped on smaller screens.

5. Dynamic Heading:

The page title (e.g., "0.5042-A — 20 Students Received Grace") must update dynamically based on the filter or rule selected by the user.