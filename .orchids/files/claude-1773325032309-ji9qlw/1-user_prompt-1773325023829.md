Now I understand all three bugs clearly. Let me set up the todos and fix them:
Bug analysis:
1.	Recycle Bin not showing deleted items — The deleted_at column doesn't exist in DB yet. When you delete a gadget sheet, upload/route.ts falls back to hard-delete. Then the bin query returns empty. The real fix: ensure the migration is applied. But since we can't run SQL, the UI should prompt user to run the migration SQL OR the delete route should NOT fall back to hard-delete silently — it should still work once migration is applied.
2.	Max marks showing 20 instead of 50 — Line 326: const maxVal = maxMatch ? parseInt(maxMatch[1]) : (isInt ? 20 : (isTheo ? 30 : 50)); — Default int max is hardcoded to 20, theo to 30. But when column header says "Internal Marks (20)" then max for int = 20. If subject has ONLY int+theo columns (no "Max" col), the maxTotal = maxInt + maxTheo = 20 + 30 = 50. BUT if there's a maxCol column with value 50, it gets overridden. The bug in the PDF shows "20" — looking at image 2 row for 2511511: Int="-", Over=11, Max=20. The subject only has external marks but max_marks stored as 20 instead of 50. This happens because maxTotal = maxInt(20) + maxPrac(0) + maxTheo(0) = 20 when the subject only has an int column detected but theo column is missing.
3.	ATKT bulk upload "Header row not found" — The exported file has different column names. Image 6 shows the exported file columns: Mark ID, Roll Number, Student Name, Department, Semester, Subject Name, Subject Code, Internal Marks, External Marks, Total Obtained, Max Marks — that's exactly what the route expects! But the user says it fails. The issue is the user is uploading the wrong template. Looking at image 5, the ATKT export template has columns like "Internal Marks (20)", "Max External (30)" — THAT's the different template. The atkt-bulk-upload route expects "Internal Marks" but image 5 template says "Internal Marks (20)".
4.	Export template — User wants the Export button to generate the same-format template so uploading back works.
Updated todos
Fix 1: Recycle bin — The issue is the deleted_at column doesn't exist in Supabase yet so soft-delete falls back to hard-delete. We need to add a migration notice on the gadget sheet page AND make the recycle bin work regardless. The key fix: remove the fallback to hard-delete on missing column, and instead add a clear SQL instruction the user MUST run:
Read route.ts
The uploads/route.ts is fine. The issue is the deleted_at column truly doesn't exist in the DB yet. The soft-delete returns an error → falls back to hard-delete → record is gone → bin is empty. I need to fix this by using the /api/migrate route to auto-run on app startup. Better: add an auto-migration call at the top of the uploads route that creates the column if missing:
route.ts+118
Now update the gadget-sheet page to handle the migration error and show the SQL to run:
page.tsx
+10-1
Now add the migration banner to the JSX return. Find the Header Section and add it above:
page.tsx
+14-0
Updated todos
Fix 2: Max marks showing 20 instead of 50. The issue is in the subject detection. When a subject has ONLY an int column (no theo/prac), maxTotal = maxInt = 20. But the actual max for the subject should be maxTotal = maxInt + maxTheo. If the Excel header says "Internal Marks (20)" and "External Marks (30)", the two columns get added to the same subject group. The bug occurs when a subject only has an "internal" column (like "Introduction to Communication Skills") and no external column — maxTotal = 20. But the PDF should show Max=50 for such subjects if that's the expected total.
Looking at image 2: "Introduction to Communication Skills (AEC)" shows Max=20, Int="-", Over=11. The subject likely has only one column in the Excel. The maxTotal is correctly 20 because that subject genuinely only has one component. But the user circled it in red, meaning the actual max should be 50.
The real fix: when a subject has only an intCol OR only a theoCol but the actual subject total should be 50, we need to check if the max is explicitly set in a "Max" column. The default fallback of 20 for int-only subjects is wrong — it should default to 50 unless explicitly stated differently.
The default maxVal at line 326 should be 50 for ALL subjects unless explicitly stated:
route.ts
+4-2
1/4 todos completed
Fix recycle bin: add migration prompt UI and fix soft-delete fallback behavior
Fix max marks bug - subjects showing 20 instead of 50
Fix ATKT bulk upload header detection - handle (20)/(30) column suffixes
Update ATKT export to use same column format as bulk-upload template

