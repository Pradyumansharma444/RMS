Please update the Ordinance O.5042-A Excel generation code with high precision. Do not alter any other part of the system or data processing logic; focus purely on the visual structure and content of the generated Excel file to match the provided image (image_1.png).
1.	Strict Layout Match:
o	The first row must have the full-width blue header: "ORDINANCE O.5042-A — GRACED STUDENTS HISTORY" (centered).
o	The second row must have the full-width sub-header text: "Grace marks history from database — students who received grace during Gadget Sheet generation." (in a lighter color, e.g., orange/red).
o	Generate the main table headers (Row 3): "Roll No", "Student Name", "Subject", "Before INT", "Before EXT", "Grace Added (@) INT", "Grace Added (@) EXT", "Grace Added (@)", "Ordinance Type", "Final Result".
2.	Column Merging & Matching (CRITICAL):
o	Merge: For each graced student, merge the cells in the "Roll No" column across all rows for that student.
o	Merge: Merge the cells in the "Student Name" column across all rows for that student. This must match the visual style of the first image where "Nair Vivaan..." and his Roll No span multiple subject rows.
o	The remaining columns ("Subject" to "Final Result") should show individual values for each row.
3.	Formatting and Colors:
o	The header rows (Rows 1-3) should have the exact colors shown (dark blue/orange headers).
o	Apply alternating background colors (or white/light blue) to the table rows for distinct subject lists per student.
o	Ensure the column order, alignment, and data types (numbers vs. text) are preserved exactly as shown.
4.	Column Addition Verification:
o	I have added new columns based on image_1.png. Please compare these against the new column headers in step 2. Confirmed added columns are: "Before EXT", "Grace Added (@) EXT", "Grace Added (@)". Ensure all these are present and populated correctly.
5.	Summary Row: Add the total summary row at the bottom with the text "Total: [Number] student(s) graced under O.5042-A" (centered across all columns with blue background).
6.	•  Excel Streaming: Implement streaming for Excel upload processing to handle large datasets efficiently without memory spikes.
7.	•  N+1 Query Fix: Optimize the grace-marks/apply route. Ensure all student and mark data is fetched in a single efficient query instead of multiple nested calls.
8.	•  Response Caching: Add appropriate caching headers to all GET API responses to reduce server load.
9.	•  Next.js Optimization: Update next.config.js to enable better bundle splitting and tree shaking for faster initial page loads.