System Logic, UI Fluidity & Database Sync Fix
"I need to finalize the RMS project by fixing critical logic, UI layout, and database synchronization issues. Please implement the following tasks to ensure the system is production-ready and follows University of Mumbai standards:

Task 1: Dynamic UI & Subject Name Overflow Fix
Issue: Long subject names (like 'INTRODUCTION TO SPORTS, PHYSICAL LITERACY...') are causing layout breaks or overlapping with marks columns (image_4a2626.png).

Requirement:

Dynamic Column Shifting: Implement a logic where if a 'Subject Name' is longer than the allocated space, the marks columns (Int, Ext, Over) must dynamically shift or adjust their position to ensure readability.

Text Wrapping: Use intelligent text-wrapping or font-scaling for exceptionally long names to prevent them from pushing columns out of the page bounds in the PDF/Grade Card.

Task 2: Strict 40% Passing & ATKT Synchronization
Logic Fix: Ensure a strict 40% passing threshold for both Internal (e.g., 8/20) and External (e.g., 12/30) heads.

F-Grade Implementation: If a student scores below 40% (like the 4 marks in image_4a26c1.png), they must be marked 'FAIL' (Grade F), unless grace marks (O.5042-A/O.5045-A) are successfully applied.

ATKT Dashboard: Ensure that any student with an 'F' grade is automatically and instantly pushed to the 'ATKT / FAIL' dashboard count (image_4a226b.png).

Task 3: Professional Excel Export & Ordinance History
Structure Update: The Ordinance History export must strictly follow the professional layout shown in WhatsApp Image 2026-03-16 at 11.39.00 PM.jpeg.

Design Requirements:

Maintain the Dark Blue/Orange color scheme for headers.

Include columns: Roll No, Name, Subject, Before Marks, Grace Added (@), Total, and Final Result (formatted as 'P A S S').

Dropdown Options: Restore the 'Export' button dropdown to offer both 'Export as XLS' and 'Export as CSV' (image_c87a3f.png).

Task 4: Database Integrity & PWA Fixes
Single-Click Delete: Fix the 'double-delete' bug where deleting a record takes two attempts. Ensure the database entry and storage file are removed in a single atomic operation.

Logo Fix: Ensure the website logo is correctly linked in the manifest.json so that 'Add to Desktop' shows the RMS icon instead of a blank square (image_b8f65b.png).

Grade Card Footer: Move the College Stamp to the bottom-left empty space, remove 'Checked By', and place the Principal/Chairperson Seal in the center-bottom area as requested.

Task 5: SGPI O.229 Bonus (Final Integration)
Automatic Benefit: Automatically add +0.1 GPA to the final SGPI for students who successfully complete CC subjects (NSS/Sports/Cultural).

Footer Tag: Render a clear '| O.229 Applied' tag in the result footer."